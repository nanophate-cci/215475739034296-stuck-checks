version: 2.1

jobs:
  tests-ci:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: Fail like Rox tests-ci
          command: |
            echo "simulated test failure (upstream job, no context)"
            exit 1

  post-test-results:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: Context job that still runs after tests-ci
          command: |
            echo "post-test-results"
            echo "context job ran; CIRCLE_JOB=$CIRCLE_JOB"

  lint:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: Context-only lint
          command: |
            echo "lint"
            echo "context job ran; CIRCLE_JOB=$CIRCLE_JOB"

workflows:
  tests_ci_pr:
    jobs:
      - tests-ci
      - post-test-results:
          context:
            - tf-rate-limit-test
          requires:
            - tests-ci: [success, failed, canceled]

  lint:
    jobs:
      - lint:
          context:
            - tf-rate-limit-test
